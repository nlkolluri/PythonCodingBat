def end_other(a, b):
  a=a.lower()
  b=b.lower()
  if a[len(a)-len(b):]==b or b[len(b)-len(a):]==a: return True
  return False
  
  #Note: I solved this without using the "endswith" method, but that is also a possible solution
